server:
  port: 8082

spring:
  application:
    name: quiz-service
  datasource:
    username: postgres
    password: 7777
    url: jdbc:postgresql://host.docker.internal:5432/quiz_service
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    serviceUrl:
      defaultZone: http://host.docker.internal:8761/eureka
